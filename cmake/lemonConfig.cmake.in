cmake_minimum_required(VERSION 3.24)
include(CMakeFindDependencyMacro)

if(NOT TARGET lemon::lemon)
  find_dependency(MPI REQUIRED COMPONENTS C)

  include("${CMAKE_CURRENT_LIST_DIR}/lemonTargets.cmake")
endif()
